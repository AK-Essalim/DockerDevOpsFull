#DevOps with Docker MOOC exercise 1.15

This is a single static webpage implemented using the nginx docker image

##Installation
```bash
docker build -t devops aessalim/devops:1.15
```
##Usage

```bash
docker run -p 80:80 devops
```
